export interface ISimpleButton {
    buttonWidth: number,
    buttonHeight: number,
    cornerRadius: number,
    label: string,
    color: string
}

export interface ISimpleInput {
    inputWidth: number,
    inputHeight: number,
    cornerRadius: number,
    label: string
}